<Application x:Class="CommunityRelay.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnMainWindowClose">
  <Application.Resources>
    <SolidColorBrush x:Key="BackgroundBrush" Color="#F5F7FA" />
    <SolidColorBrush x:Key="CardBrush" Color="#FFFFFF" />
    <SolidColorBrush x:Key="AccentBrush" Color="#275DAD" />
    <SolidColorBrush x:Key="MutedBrush" Color="#5C6675" />
    <Style TargetType="Button">
      <Setter Property="Padding" Value="14,8" />
      <Setter Property="Margin" Value="0,0,8,8" />
      <Setter Property="MinWidth" Value="96" />
    </Style>
    <Style TargetType="TextBox">
      <Setter Property="Padding" Value="7" />
      <Setter Property="Margin" Value="0,3,0,10" />
    </Style>
    <Style TargetType="PasswordBox">
      <Setter Property="Padding" Value="7" />
      <Setter Property="Margin" Value="0,3,0,10" />
    </Style>
    <Style TargetType="CheckBox">
      <Setter Property="Margin" Value="0,4,0,7" />
    </Style>
  </Application.Resources>
</Application>
